# Paste real keys into .env (never committed). One key per line, no quotes.
HYDRA_DB_API_KEY=
PIPESHIFT_API_KEY=
# optional: override if your key has a different model provisioned
PIPESHIFT_MODEL=
ROCKETRIDE_API_KEY=
INSFORGE_API_KEY=
